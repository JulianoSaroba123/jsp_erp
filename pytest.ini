[pytest]
# Root-level guardrail: when pytest is executed from repository root,
# collect only the official backend suite.
testpaths = backend/tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

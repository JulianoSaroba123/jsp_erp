# ============================================================================
# .env.test.example - Template para ambiente de testes
# ============================================================================
# INSTRUÇÕES:
# 1. Copie este arquivo: cp .env.test.example .env.test
# 2. Ajuste as credenciais para seu ambiente local
# 3. NUNCA commite .env.test (deve estar no .gitignore)
# ============================================================================

# Database de testes (PostgreSQL)
DATABASE_URL_TEST=postgresql://jsp_user:Admin123@localhost:5432/jsp_erp_test

# JWT Secret (pode ser qualquer string em dev)
SECRET_KEY=dev_secret_key_for_testing_only_change_in_production

# CORS (para testes pode ser *)
CORS_ALLOW_ORIGINS=*

# Ambiente
ENVIRONMENT=test

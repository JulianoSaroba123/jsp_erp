# =============================================================================
# ERP JSP - Staging Environment Template
# =============================================================================
# Copie este arquivo e configure as variáveis antes do deploy
# NUNCA commite este arquivo com valores reais no Git!
# =============================================================================

# -----------------------------------------------------------------------------
# CORE SETTINGS
# -----------------------------------------------------------------------------

# Environment: local, development, production, test
# Use "production" para staging (mesmo sendo staging, usa configs de produção)
ENV=production

# Database Connection (PostgreSQL)
# Exemplo Render: postgresql://user:pass@dpg-xxxxx.oregon-postgres.render.com/dbname
DATABASE_URL=postgresql://user:password@host:5432/dbname

# JWT Secret Key (OBRIGATÓRIO - GERE UMA CHAVE ÚNICA!)
# Gerar: python -c "import secrets; print(secrets.token_urlsafe(64))"
SECRET_KEY=CHANGE_ME_GENERATE_SECURE_KEY

# -----------------------------------------------------------------------------
# CORS CONFIGURATION
# -----------------------------------------------------------------------------

# Origens permitidas (CSV - sem espaços)
# Exemplo: https://app.exemplo.com,https://admin.exemplo.com
CORS_ALLOW_ORIGINS=https://seu-frontend.onrender.com

# -----------------------------------------------------------------------------
# OPTIONAL SETTINGS
# -----------------------------------------------------------------------------

# Debug mode (default: false)
# NUNCA use true em produção/staging!
DEBUG=false

# JWT Token expiration in minutes (default: 60)
ACCESS_TOKEN_EXPIRE_MINUTES=60

# -----------------------------------------------------------------------------
# RENDER-SPECIFIC (se usando Render.com)
# -----------------------------------------------------------------------------

# Render automatically provides:
# - PORT (não precisa configurar)
# - RENDER=true
# - RENDER_SERVICE_NAME
# - RENDER_INSTANCE_ID

# -----------------------------------------------------------------------------
# VALIDATION CHECKLIST
# -----------------------------------------------------------------------------
# Antes de fazer deploy, verifique:
# [ ] SECRET_KEY foi gerada (não é CHANGE_ME)
# [ ] DATABASE_URL aponta para banco correto
# [ ] CORS_ALLOW_ORIGINS tem domínios válidos (não *)
# [ ] ENV=production
# [ ] DEBUG=false (ou omitido)
# -----------------------------------------------------------------------------

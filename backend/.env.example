# ============================================================================
# CONFIGURAÇÃO DO ERP JSP - Backend
# ============================================================================
# Copie este arquivo para .env e ajuste os valores

# ----------------------------------------------------------------------------
# ENVIRONMENT (OBRIGATÓRIO)
# ----------------------------------------------------------------------------
# Define o ambiente de execução
# Valores permitidos: development | production
ENVIRONMENT=development

# ----------------------------------------------------------------------------
# DATABASE
# ----------------------------------------------------------------------------
# URL de conexão PostgreSQL (formato SQLAlchemy)
# postgresql+psycopg://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL=postgresql+psycopg://jsp_user:jsp123456@localhost:5432/jsp_erp

# ----------------------------------------------------------------------------
# SECURITY / JWT (OBRIGATÓRIO)
# ----------------------------------------------------------------------------
# Chave secreta para assinar tokens JWT
# ⚠️ CRÍTICO: Esta chave NUNCA pode usar o valor padrão!
# ⚠️ Em produção, use uma chave LONGA (64+ caracteres) e ALEATÓRIA!
# 
# Para gerar uma chave forte:
#   python -c "import secrets; print(secrets.token_urlsafe(64))"
#
# ❌ NUNCA commitar a chave real no Git!
# ❌ NUNCA usar 'your-secret-key-change-in-production'!
SECRET_KEY=your-secret-key-change-in-production-use-long-random-string

# Algoritmo JWT (não mudar a menos que saiba o que está fazendo)
ALGORITHM=HS256

# Expiração do token de acesso (minutos)
ACCESS_TOKEN_EXPIRE_MINUTES=60

# ----------------------------------------------------------------------------
# CORS (OBRIGATÓRIO EM PRODUÇÃO)
# ----------------------------------------------------------------------------
# Lista de origins permitidas (separadas por vírgula, SEM espaços)
# 
# ⚠️ Em PRODUCTION: Esta variável é OBRIGATÓRIA e não pode estar vazia!
# ⚠️ NUNCA usar "*" em produção!
#
# Exemplos:
#   Development: (deixar vazio para permitir localhost)
#     CORS_ALLOW_ORIGINS=
#
#   Production: (especificar domínios exatos)
#     CORS_ALLOW_ORIGINS=https://app.exemplo.com,https://admin.exemplo.com
#
CORS_ALLOW_ORIGINS=

# ----------------------------------------------------------------------------
# APPLICATION
# ----------------------------------------------------------------------------
# Modo debug (True/False)
# ⚠️ NUNCA ativar DEBUG=True em produção!
DEBUG=True

# Nome da aplicação
APP_NAME=ERP JSP

# Versão
APP_VERSION=1.0.0

# ----------------------------------------------------------------------------
# PAGINAÇÃO
# ----------------------------------------------------------------------------
DEFAULT_PAGE_SIZE=20
MAX_PAGE_SIZE=100
